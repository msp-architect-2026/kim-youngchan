{{- if .Values.authService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: auth-service
    app.kubernetes.io/part-of: dropx
spec:
  replicas: {{ .Values.authService.replicas }}
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ .Values.config.metricsPort | quote }}
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: dropx-app-sa
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecret }}
      containers:
        - name: auth-service
          image: "{{ .Values.authService.image }}:{{ .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: 8000
            - name: metrics
              containerPort: 8001
          env:
            # ── MySQL (auth-service 키 형식: snake_case) ──────────────
            - name: mysql_host
              valueFrom:
                configMapKeyRef:
                  name: dropx-config
                  key: MYSQL_HOST
            - name: mysql_port
              valueFrom:
                configMapKeyRef:
                  name: dropx-config
                  key: MYSQL_PORT
            - name: mysql_db
              valueFrom:
                configMapKeyRef:
                  name: dropx-config
                  key: MYSQL_DB
            - name: mysql_user
              valueFrom:
                secretKeyRef:
                  name: dropx-secret
                  key: MYSQL_USER
            - name: mysql_password
              valueFrom:
                secretKeyRef:
                  name: dropx-secret
                  key: MYSQL_PASSWORD
            # ── Redis ────────────────────────────────────────────────
            - name: redis_host
              valueFrom:
                configMapKeyRef:
                  name: dropx-config
                  key: REDIS_HOST
            - name: redis_port
              valueFrom:
                configMapKeyRef:
                  name: dropx-config
                  key: REDIS_PORT
            # ── JWT ──────────────────────────────────────────────────
            - name: JWT_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: dropx-secret
                  key: JWT_SECRET_KEY
            # ── 공통 ─────────────────────────────────────────────────
            - name: APP_ENV
              valueFrom:
                configMapKeyRef:
                  name: dropx-config
                  key: APP_ENV
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: dropx-config
                  key: LOG_LEVEL
            - name: METRICS_PORT
              valueFrom:
                configMapKeyRef:
                  name: dropx-config
                  key: METRICS_PORT
          resources:
            requests:
              cpu: {{ .Values.authService.resources.requests.cpu | quote }}
              memory: {{ .Values.authService.resources.requests.memory | quote }}
            limits:
              cpu: {{ .Values.authService.resources.limits.cpu | quote }}
              memory: {{ .Values.authService.resources.limits.memory | quote }}
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: auth-service
    app.kubernetes.io/part-of: dropx
spec:
  selector:
    app: auth-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
    - name: metrics
      port: 8001
      targetPort: 8001
{{- end }}
